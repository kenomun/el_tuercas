<%= form_with(model: servicio) do |form| %>
  <% if servicio.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(servicio.errors.count, "error") %> prohibited this servicio from being saved:</h2>
      <ul>
        <% servicio.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="mb-3">
    <%= form.label :nombre, class: "form-label"  %>
    <%= form.text_field :nombre, class: "form-control"  %>
  </div>
  <div class="mb-3">
    <%= form.label :Descripción, style: "display: block"  %>
    <%= form.text_area :descripcion, class: "form-control"  %>
  </div>
  <div class="d-flex justify-content-between ">
    <%= link_to "Volver a la página principal", servicios_path, class: "btn btn-color mt-3" %>
    <%= form.submit "Guardar", class: "btn btn-success mt-3" %>
  </div>
<% end %>
